---
import BaseLayout from "./BaseLayout.astro";
import TagButton from "../components/TagButton.astro";
import SequenceNav from "../components/SequenceNav.astro";
import LocalGraphContainer from "../components/LocalGraphContainer.astro";
 
const {frontmatter} = Astro.props;
---
<BaseLayout pageTitle = {frontmatter.title}>
    <div class="frontmatter-container">
        <div class="tag-button-container">{frontmatter.tags.map((tag) => <TagButton tag={tag} url={`/tags/${tag}`}/>)}</div>
        <p class="frontmatter">type: {frontmatter.type}</p>
        <p class="frontmatter">created on: {frontmatter.date.toString().slice(0,10)}</p>
    </div>
    <slot />
    <LocalGraphContainer />
    <SequenceNav projectId={frontmatter.projectId} sequence={frontmatter.sequence} />
</BaseLayout>